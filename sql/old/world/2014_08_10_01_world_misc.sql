- fix for haste damage bug  is not the right way but it works so we close the  issues from the forum about that so far 
UPDATE  smart_scripts SET action_param1=0 WHERE action_param1=55095;
UPDATE  smart_scripts SET action_param1=0 WHERE action_param1=32717;
